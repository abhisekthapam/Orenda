import 'package:flutter/material.dart';

class OnBoardingOrenda extends StatelessWidget {
  const OnBoardingOrenda({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Onboarding Orenda'),
    );
  }
}